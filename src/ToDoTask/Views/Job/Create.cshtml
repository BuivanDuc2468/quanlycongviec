@model ToDoTask.Data.Entities.Job

@{
    ViewData["Title"] = "Create";
    Layout = "_Layout";
}

<h1>Tạo mới</h1>

<h4>Công việc</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group w-100">
                <label asp-for="ProjectId" class="control-label"></label>
                <select class="js-select2 w-100" name="ProjectId" onchange="eventChangePrjId()" id="ProjectSelected" asp-items="@(new SelectList(ViewBag.Project, "ProjectId", "ProjectName"))"></select>
                <span asp-validation-for="ProjectId" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <label asp-for="Content" class="control-label"></label> 
                <input type="text" asp-for="Content" class="form-control" multiple />
                <span asp-validation-for="Content" class="text-danger"></span>
            </div>
            <div class="form-group mt-3 w-100">
                <label asp-for="UserId" class="control-label"></label>
                <select class="js-select2 w-100" asp-for="UserId" id="UserVal"></select>
                <span asp-validation-for="UserId" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <label asp-for="DateLine" class="control-label"></label>
                <input asp-for="DateLine" class="form-control" />
                <span asp-validation-for="DateLine" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Lưu" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Quay lại</a>
</div>
@section Scripts{
    <script>
        $(document).ready(function () {
            eventChangePrjId();
            $('.js-select2').select2();
        });
        function eventChangePrjId() {
            let ProjectSelected = $("#ProjectSelected").val();
            $.ajax({
                url: location.origin + "/Job/loadUserOfProject/?projectId=" + ProjectSelected,
                type: "post",
                dataType: 'json',
                success: function (data) {
                    var options = '';
                    for (var x = 0; x < data.length; x++) {
                        options += '<option value="' + data[x].id + '">' + data[x].name + '</option>';
                    }
                    $('#UserVal').html('');
                    $('#UserVal').html(options);
                    $('.js-select2').select2();
                },
                error: function (e) {
                    console.log(e.message);
                }
            });
        }
    </script>
}

